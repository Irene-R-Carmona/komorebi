{
  "scripts": {
    "build:charts": "esbuild public/js/chart-bundle.js --bundle --minify --outfile=public/js/charts.min.js --format=iife --global-name=ChartBundle",
    "test:e2e": "playwright test tests/e2e",
    "test:a11y": "playwright test tests/e2e/accessibility",
    "test:api": "newman run docs/openapi.postman.json --environment docs/postman-env.json --reporters cli",
    "lighthouse": "lhci autorun"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.58.2",
    "chart.js": "^4.4.1",
    "esbuild": "^0.28",
    "newman": "^6.2.1"
  }
}

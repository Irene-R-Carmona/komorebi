; Deshabilita OPcache en el entorno de test.
;
; Motivo: pcov y opcache son incompatibles en PHP 8.4 —
; al cargarlos juntos PHP emite un fatal error.
;
; En dev (Dockerfile.dev) opcache sí está activo con validate_timestamps=1.

opcache.enable     = 0
opcache.enable_cli = 0
